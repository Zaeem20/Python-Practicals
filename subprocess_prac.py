""" 
Subprocess in Python,

Subprocess is a built-in library for interacting between IO streams, and access it
It is also used for Spawning and interacting with process, you can use subprocess 
if you want to execute some commands in your Shell with run().

Written by © Zaeem Durani
"""

import subprocess

subprocess.run(['python', 'shinchan.py'])